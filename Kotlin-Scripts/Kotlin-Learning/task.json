{
    "version": "2.0.0",
    "tasks": [
        {
            "label": "build",
            "type": "shell",
            "command": "kotlinc test.kt -include-runtime -d foo.jar && java -jar foo.jar"
        }
    ]
}